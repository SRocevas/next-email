module.exports = function() {

  var config = {
    transportOptions: {
      service: 'gmail', 
      auth: {
        user: '',
        pass: ''
      }
    },
    mailOptions: {
      to: 'test@gmail.com', // Default address(es) to send test emails to (can be comma separated)
      from: 'Gulp Test <youremail@gmail.com>', // Sender details 
      subject: 'Test email - sent by Next-Email' // Default subject line
    },
    imageHost: '' // Full url path to your image host, with a trailing slash.
  };
  return config;
}